# Quality audit of the report — 2026-08-25

> **SELF-AUDIT, NOT A BLIND PANEL.** The session was under an explicit constraint not to open
> independent sub-agents, so the three fresh-context auditors of the house precedent did not run.
> The person who scored is the person who wrote. That is a **declared degradation, not an
> equivalence**: the score measures house quality of the text as judged by an interested party.
> Recommendation on record: a blind three-auditor panel before any external claim of quality, at
> the journal-submission pass. The deposit itself remains human-gated.
>
> Calibration **frozen before scoring**: `docs/paper/QUALITY-CALIBRATION.md`.
> Mechanical facts: `docs/paper/quality-checks-report.json`.

## Denominator

**1000. Zero sub-criteria excluded.** Nothing was marked not-applicable. The rubric's conditionals
("when applicable", "when required") were read and declared in the calibration BEFORE scoring, all
of them as APPLICABLE. Trimming a denominator is how a self-audit congratulates itself; none was
trimmed here.

## Command and mechanical output

```bash
python3 code/quality_checks.py
```

```text
-> docs/paper/quality-checks-report.json
-> verdict: PASS (0 failures)
```

| Density metric | `MANUSCRIPT-en.md` | `MANUSCRITO-pt-BR.md` | Threshold |
|---|---|---|---|
| Words | 4,081 | 4,129 | — |
| Unique technical concepts (of 66 in the lexicon) | 62 | 62 | — |
| **TDI** | **7.60** | **7.51** | >= 3.0 PASS |
| **Filler ratio** | **0.0000** | **0.0002** | < 0.12 PASS |
| **Info density** | **5.27** | **5.21** | >= 0.15 PASS |
| Inline citations | 11 | 11 | — |

Non-numeric gates, all passing: 0 placeholders · 12 references cited / 12 entries / in sequence ·
9 hashes checked against `shasum -a 256` of the files on disk · identical section skeletons across
editions · 4/4 cited URLs HTTP 200 · 9/9 DOIs resolving at Crossref · 12 quantities recomputed from
the sealed JSONs plus 5 identifiers checked against the sealed sets plus 4 per-term record counts.

## Score by sub-criterion

### 1. Formal structure — 138/150

| Sub-criterion | Max | Score | Evidence / deduction |
|---|---|---|---|
| Clear thesis in the first paragraph | 30 | 25 | Paragraph 1 of section 1 sets the scene and closes on "an empty result is informative"; the thesis proper ("that inference is only valid if the search actually searched") lands in paragraph 2. The abstract carries it in full. **-5** for the one-paragraph delay. |
| Consistent numbering | 20 | 20 | Sections 1-6, 2.1-2.5, 3.1-3.5, 4.1-4.4; skeleton identical across both files (gate `pair_parity`). |
| Descriptive subtitle | 15 | 15 | Two-part title with a colon; the second part names the object and the number of defects. |
| Bilingual abstract, perfect mirror | 25 | 25 | Assessed at the deposit level (one DOI, two files): the abstract and the resumo carry the same four-block structure and the same numbers — the `sealed_numbers` gate requires every quantity in BOTH files. |
| Keywords | 10 | 10 | Six terms, `;`-separated, lower case except proper nouns (ReBEC, WHO ICTRP). |
| Dense table when required | 20 | 16 | Five tables. The term-by-term table in section 3.3 is **6 rows x 5 columns** — inside the 4-7 x 3-5 target. **-4:** the other four carry only 2-3 columns. |
| Hanging indent in references | 10 | 10 | All 12 entries use indented continuation lines. |
| Diagram or figure | 20 | 17 | Figure 1, vector SVG, generated by a released deterministic script and hashed in section 6, in both languages. **-3:** only defect 1 has a figure; defects 2 and 3, which carry the strongest numbers, have none. |

### 2. Technical density — 238/250 (EN) · 237/250 (PT)

| Sub-criterion | Max | Score | Evidence / deduction |
|---|---|---|---|
| TDI >= 3 | 80 | 80 | 7.60 / 7.51 — 2.5x the floor, over 62 of the 66 concepts of a lexicon written **by domain**, not swept from the manuscript. |
| Filler < 12% | 60 | 60 / 59 | EN 0.0000. PT 0.0002 (one occurrence of *de forma*): **-1** on the Portuguese edition. |
| Info density >= 0.15 | 50 | 50 | 5.27 / 5.21. |
| Specific vocabulary | 30 | 30 | The empty-adjective regex (robust/modern/efficient/scalable/...) returns zero in both files. |
| Latin used with purpose (2-4x) | 10 | **0** | **Zero occurrences.** Full, deliberate deduction: inserting decorative Latin to score would be exactly the filler the ruler exists to catch. |
| Quantitative metric per subsection | 20 | 18 | Every leaf subsection carries a measured quantity. **-2:** the one in section 2.1 is spelled out ("six terms", two named positive controls) rather than given as a numeral. |

Technical density 238 >= 100, so **the hard cap of 600 does not fire.**

### 3. Theoretical grounding — 131/150

| Sub-criterion | Max | Score | Evidence / deduction |
|---|---|---|---|
| Reference count (floor 10) | 30 | 30 | 12. The floor was fixed in the calibration at 10 when the manuscript had 7 — the ruler was not shaped to the artefact. |
| Distinct source types >= 5 | 30 | 28 | journal paper (6) · RFC (9525, 6797) · book (Manning et al., CUP) · framework documentation (the DataTables manual) · agency standard (WHO ICTRP, W3C). **-2:** an adversarial reviewer could merge "RFC" and "W3C CR" into a single "standard" type; five would still remain, but the margin is thin. |
| Mean E-E-A-T >= 75 | 40 | 37 | BMJ, Rev Saude Publica, Rev Panam Salud Publica (PAHO), Sao Paulo Med J, J Particip Med, WHO, W3C, IETF x2, Cambridge University Press. **-3:** the DataTables manual is the only item below the floor — cited solely for the one thing it is primary authority on, its own parameter contract, and verified by reading the served page. |
| Citation in the first paragraph | 15 | 15 | [1] and [2] both in section 1, paragraph 1. |
| References from the last 3 years >= 50% | 20 | **6** | Dated 2023-2026: 2 of 12 (RFC 9525, 2023; Woolley et al., 2025) = 17%; counting the three continuously maintained live documents (WHO, W3C CR, DataTables), 5 of 12 = 42%. **Fails under either way of counting.** Accepted as genre honesty: the load-bearing literature here — registry methodology (2009-2018) and information-retrieval fundamentals (2008) — is old because it is right, and swapping it for recent but weaker sources would buy the metric with correctness. |
| No banned references | 15 | 15 | No Wikipedia, no unattributed blog. |

### 4. Originality and thesis — 185/200

| Sub-criterion | Max | Score | Evidence / deduction |
|---|---|---|---|
| Arguable thesis | 60 | 56 | "A WHO primary registry can serve a search that does not query its own database and fails invisibly" — contestable in good faith (an operator could argue the web index is adequate, or that recall on one term generalises nothing). **-4:** the thesis rests on recall measured for a single term. |
| Named trade-off, made explicit | 50 | 42 | **precision vs recall**, named and used in section 3.4 with [9] (the three omissions are recall, the two extras are precision, and only the first was measured); **fail-silent vs fail-fast**, named in section 4.1. **-8:** the pair is named and used, not *measured* — there is no quantified frontier. |
| Non-obvious insight | 40 | 38 | Two: "an empty result is informative" as the property that makes registry search fragile; and the archival consequence — the Internet Archive permanently holds a search page that never filtered, so future reconstruction of what was searchable is corrupted at the source. |
| Central elegant solution | 30 | 29 | The two-armed protocol (positive control plus response-identity test): one cheap test that separates "there is nothing" from "the search did not run", and exactly what generalises to other registries. |
| Numbered future directions | 20 | 20 | Section 4.4: (i) the ICTRP census · (ii) cross-browser replication and mechanism isolation · (iii) measuring the downstream effect. |

### 5. Internal consistency — 98/100

| Sub-criterion | Max | Score | Evidence / deduction |
|---|---|---|---|
| No contradiction between sections | 40 | 39 | The open attribution in section 3.2 is restated with the same force in limitation 1 of section 4.3; the headline 14/17 and the declared exclusion of `RBR-5vpyh4` do not collide. |
| Alignment with the abstract | 20 | 20 | Every claim in the abstract (three defects, 14/17, >= 11 months, no claim of intent) reappears with its evidence in section 3. |
| Reference numbering coherent | 20 | 20 | Gate `references`: 12 cited, 12 entries, in sequence, in both files. **The orphan [6] caught on the gate's first run was fixed** — precisely the kind of defect human reading misses. |
| Consistent vocabulary | 20 | 19 | "public search" / "registry database" / "defect 1-2-3" used stably; no double spelling. |

### 6. Factual correctness — 150/150

| Sub-criterion | Max | Score | Evidence |
|---|---|---|---|
| Claims with verified citation | 60 | 60 | Every load-bearing quantity is recomputed from a sealed JSON by the gate; the two human-observed values the report publishes ("approximately 38", 69,876 bytes) are traced to the document that attests them and declared as such; a third, the number of result URLs paged through, was withdrawn from the report rather than traced, because it was never sealed. |
| No invention | 40 | 40 | All 12 entries come from tool output: Europe PMC `resultType=core`, `rfc-editor.org/rfc/*.json`, Crossref. `search[value]` verified by reading the served manual page. |
| Assumptions marked | 25 | 25 | The `http`->`https` attribution is declared unisolated in section 3.2 and again in limitation 1; the between-days byte variance in limitation 4; the Europe PMC counts declared co-occurrence, not evidence of searching. |
| URLs reachable | 15 | 15 | 4/4 HTTP 200 (network gate). |
| DOIs valid | 10 | 10 | 9/9 resolve at Crossref (network gate). |

## Result

| File | Total | Gate >= 920 |
|---|---|---|
| `docs/paper/MANUSCRIPT-en.md` | **940/1000** | PASS |
| `docs/paper/MANUSCRITO-pt-BR.md` | **939/1000** | PASS |

Technical density 238 / 237 (>= 100), so the hard cap does not fire.

## What this score does not mean

1. **It is not peer review and not a blind panel.** See the notice at the top. 940 here is not 940
   from a third party.
2. **The rubric audited the TEXT.** It does not judge whether the finding matters, whether the
   venue is right, or whether the notification to the operator is well designed.
3. **The two largest deductions are real and stand:** reference recency (6/20) and the absence of
   Latin (0/10). Neither will be "fixed" with padding.
4. **One pending item is recorded as a decision, not an oversight:** four neighbouring PMIDs raised
   in an internal adversarial review remain unverified and are deliberately kept OUT of the text;
   they are to be verified at the journal-submission pass, when a reviewer would cite them.
